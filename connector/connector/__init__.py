"""aw-mcp-stdio-wrapper — runs a local stdio MCP server and dials out to a
gateway's /link endpoint so it can be reached over HTTP without opening any
inbound port (see the top-level README's "connector" section)."""
